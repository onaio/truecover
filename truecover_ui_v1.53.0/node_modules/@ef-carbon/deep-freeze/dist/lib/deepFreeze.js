export function deepFreeze(o) {
    for (const value of Object.values(o)) {
        if (!Object.isFrozen(value)) {
            Object.freeze(value);
        }
    }
    return Object.isFrozen(o) ? o : Object.freeze(o);
}
export default deepFreeze;
//# sourceMappingURL=deepFreeze.js.map